<Project>
  <PropertyGroup>
    <RepoRoot Condition="'$(RepoRoot)' == ''">$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)..\'))</RepoRoot>
    <KitLibProductId Condition="'$(KitLibProductId)' == ''">KitLib</KitLibProductId>
    <RepoBuildDir>$(RepoRoot)build\$(ModId)</RepoBuildDir>
    <PublishDir>$(RepoBuildDir)</PublishDir>
    <OutputPath>$(RepoBuildDir)\</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
  </PropertyGroup>

  <Target Name="ForceModOutputPath" BeforeTargets="CoreCompile">
    <PropertyGroup>
      <RepoBuildDir>$(RepoRoot)build\$(ModId)</RepoBuildDir>
      <PublishDir>$(RepoBuildDir)</PublishDir>
      <OutputPath>$(RepoBuildDir)\</OutputPath>
      <OutDir>$(RepoBuildDir)\</OutDir>
      <IntermediateOutputPath>$(RepoBuildDir)\obj\</IntermediateOutputPath>
    </PropertyGroup>
    <MakeDir Directories="$(RepoBuildDir);$(RepoBuildDir)\obj" />
  </Target>

  <Target Name="StageSatelliteToBundle" AfterTargets="Build">
    <PropertyGroup>
      <_KitLibProductModules>$(RepoRoot)build\$(KitLibProductId)\modules</_KitLibProductModules>
    </PropertyGroup>
    <MakeDir Directories="$(_KitLibProductModules)" />
    <Copy
      SourceFiles="$(TargetPath)"
      DestinationFiles="$(_KitLibProductModules)\$(AssemblyName).dll"
      SkipUnchangedFiles="true"
      Condition="Exists('$(TargetPath)')" />
    <!-- Drop stale satellites left in KitLib/modules from the pre-split layout. -->
    <ItemGroup Condition="'$(KitLibProductId)' == 'KitLib'">
      <_StaleKitLibModule Include="$(RepoRoot)build\KitLib\modules\*.dll"
                         Condition="Exists('$(RepoRoot)build\KitLib\modules')" />
      <_StaleKitLibModule Remove="$(RepoRoot)build\KitLib\modules\KitLib.User.dll" />
      <_StaleKitLibModule Remove="$(RepoRoot)build\KitLib\modules\KitLib.Cheat.dll" />
    </ItemGroup>
    <Delete Files="@(_StaleKitLibModule)" TreatErrorsAsWarnings="true" />
  </Target>

  <Target Name="StripGodotSdkArtifacts" BeforeTargets="CoreCompile">
    <ItemGroup>
      <Analyzer Remove="@(Analyzer)" Condition="$([System.String]::Copy('%(Identity)').Contains('Godot'))" />
      <Compile Remove="@(Compile)" Condition="$([System.String]::Copy('%(Identity)').Contains('Godot.SourceGenerators'))" />
      <Compile Remove="@(Compile)" Condition="$([System.String]::Copy('%(Identity)').Contains('GodotPlugins'))" />
    </ItemGroup>
  </Target>
</Project>
