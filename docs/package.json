{
  "name": "devmode-docs",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "build": "pnpm run build:ssg",
    "build:spa": "valaxy build",
    "build:ssg": "node scripts/sync-changelog.mjs && valaxy build --ssg",
    "dev": "node scripts/sync-changelog.mjs && valaxy",
    "serve": "vite preview"
  },
  "dependencies": {
    "valaxy": "^0.28.4",
    "valaxy-theme-nova": "^0.3.1"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
