# 设置

- 游戏速度、跳过动画、UI **主题**
- **轨道布局**：拖拽排序、隐藏不需要的标签；可重置默认
- 显示隐藏卡牌等全局选项（与卡牌浏览器 Library 行联动）
- **进度保护**：mod 集变化时自动备份 `progress.save`；可选对已卸载 mod 残留进度仅写日志警告。标题画面 **DEVMODE → 进度保护** 可一键恢复（仅主菜单；建议 mod 集与备份时一致）。
